server:
  port: 8081
spring:
  application:
    name: spring-cloud-config-client
  profiles:
    active: native
  # 配置中心
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/